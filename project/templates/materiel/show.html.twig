{% extends 'base.html.twig' %}

{% block title %}Materiel{% endblock %}

{% block body %}
    <h1>Materiel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ materiel.name }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ materiel.reference }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ materiel.getFormatedPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_app') }}">Retour à l'accueil</a>

    <a href="{{ path('app_materiel_edit', {'id': materiel.id}) }}">Modifier</a>

    {{ include('materiel/_delete_form.html.twig') }}
{% endblock %}
